# キャラクター選択画面背景セットアップガイド

## 背景画像の配置

提供された5人のヒーローが描かれたキャラクター選択画面の画像を以下の場所に配置してください：

```
mathmagic/
  assets/
    images/
      character-select-bg.jpg  ← ここに配置
```

## 画像ファイル詳細

- **ファイル名**: `character-select-bg.jpg` （または `.png`）
- **推奨サイズ**: 1920x1080px 以上（提供画像は適切なサイズ）
- **フォーマット**: JPG または PNG
- **特徴**:
  - 5人のキャラクターポートレート（金の額縁）
  - **The Paladin**: 騎士（左端）
  - **The Shadow**: 盗賊/ローグ（左から2番目）
  - **The Arcanist**: 魔術師（中央・大きめ）
  - **The Ranger**: エルフの射手（右から2番目）
  - **The Dwarf King**: ドワーフの戦士（右端）
  - 夕暮れの幻想的な風景（紫とピンクの空）
  - 浮遊する城、山、森、川
  - 「BEGIN JOURNEY」ボタン（装飾枠付き）
  - 左右に紫色のルーン石柱

## 画像が異なるファイル名の場合

もし画像のファイル名が異なる場合は、`css/style.css` の以下の行を修正してください：

```css
/* Line 1283 付近 */
background-image: url('../assets/images/character-select-bg.jpg');
```

例：`your-character-select.png` の場合
```css
background-image: url('../assets/images/your-character-select.png');
```

## デザイン仕様

### 背景の表示方法
- **表示**: `background-size: cover` - 画面全体をカバー
- **位置**: `background-position: center` - 中央配置
- **固定**: `background-attachment: fixed` - スクロール時に背景は固定
- **透明度**: `opacity: 0.80` - 80%の不透明度（キャラクターカードの視認性を確保）
- **ベース色**: `#2a2340` - 濃い紫（画像読み込み前の背景色）

### 透明度について

キャラクター選択画面は既にポートレートが描かれているため、実際のキャラクターカードとの調和を考慮して80%に設定しています。

### レスポンシブ対応
- スマホ〜PCまで自動で画面サイズに適応
- 背景は常に中央に配置され、画面をカバー
- スクロール可能（overflow-y: auto）

## カスタマイズ

### 背景の透明度を調整

キャラクターカードの視認性を調整したい場合は、`css/style.css` の `opacity` を変更：

```css
.character-select-bg::before {
    opacity: 0.80;  /* 0.5（薄い）〜 1.0（濃い） */
}
```

- **0.6**: 背景がかなり薄く、カードが強調される
- **0.80**: デフォルト（バランス型）
- **0.90**: 背景がやや濃い
- **1.0**: 背景がくっきり見える（カードが見づらくなる可能性）

### 背景色を調整

画像読み込み前やエラー時に表示される背景色：

```css
.character-select-bg {
    background: #2a2340;  /* 濃い紫 */
}
```

夕暮れの空の雰囲気に合わせた紫色を選択済みです。

## ファイル構成

```
mathmagic/
├── assets/
│   └── images/
│       ├── fantasy-start-bg.jpg         # スタート画面背景
│       ├── world-map-bg.jpg             # ワールドマップ背景
│       ├── battle-bg.jpg                # バトル画面背景
│       └── character-select-bg.jpg      # キャラクター選択背景 ← NEW!
├── css/
│   └── style.css                        # 背景スタイル追加済み
├── character-select.html                # class="character-select-bg" に変更済み
└── CHARACTER_SELECT_SETUP.md           # このファイル
```

## 使用場面

この背景は以下の画面で使用されます：
- **character-select.html**: プレイヤー（キャラクター）選択画面
- 既存プレイヤーから選択、または新規作成へ遷移

## UI要素との調和

### キャラクターカード
- DQ風のウィンドウデザイン
- 金色の枠線とグラデーション背景
- キャラクター情報（名前、学年、レベル、経験値）

### ヘッダー
- DQ風ウィンドウ（`dq-window`クラス）
- 金色のテキスト（`dq-gold-text`クラス）

### 星のエフェクト
- 既存の星のパーティクルアニメーション
- 背景画像の夕暮れの空と調和

## デザインコンセプト

### 物語の始まり

この画面は「冒険の始まり」を演出します：

1. **選択の重み**: 5人のヒーローから選ぶ重要な瞬間
2. **旅立ち**: 「BEGIN JOURNEY」のメッセージ
3. **幻想世界**: 浮遊する城や幻想的な風景
4. **夕暮れ**: 冒険の前夜、期待と緊張

### キャラクタータイプの多様性

背景に描かれた5人のヒーロー：
- **騎士（Paladin）**: 正義と守護
- **盗賊（Shadow）**: 機敏さと策略
- **魔術師（Arcanist）**: 魔法と知識（中央で強調）
- **射手（Ranger）**: 自然と正確さ
- **ドワーフ（Dwarf King）**: 力と忍耐

マスマジ！のキャラクタータイプ（wizard, warrior, knight, archer）と呼応するデザインです。

## トラブルシューティング

### 背景が表示されない
1. 画像ファイルが正しいパスに配置されているか確認
   - `assets/images/character-select-bg.jpg`
2. ファイル名の大文字小文字を確認
3. ブラウザのキャッシュをクリア（Ctrl + Shift + R）
4. 開発者ツールのコンソールでエラーを確認

### 背景が暗すぎる／明るすぎる
- `opacity` の値を調整（上記「背景の透明度を調整」参照）
- 推奨範囲: 0.75〜0.90

### キャラクターカードが見づらい
- 透明度を下げる（例: `opacity: 0.70`）
- カード自体の背景やボーダーを調整することも可能

### 背景のキャラクターとカードが混在して見える
- 透明度を下げる（例: `opacity: 0.65`）
- または、カードに強めのドロップシャドウを追加

## 統合された世界観

この背景は、マスマジ！の全体的なストーリーフローに統合されています：

1. **スタート画面**: 魔法の世界への招待
2. **キャラクター選択**: ヒーローの選択と旅立ち ← この画面
3. **ワールドマップ**: 冒険の舞台を探検
4. **バトル画面**: 実際の戦いと試練

夕暮れの空は「冒険の前夜」を象徴し、プレイヤーに「これから始まる冒険」への期待を抱かせます。

## ブラウザ対応

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

**更新日**: 2024-11-08
**デザイナー**: Claude Code
